<% if !@printable %>
  <%= link_to "Comments (#{@card.comments.count})", @card
  # Could alternatively be pluralize(@card.comments.count, "comment")
  %>
  <% if permission_to?(:edit, @cardset) %>
   | <%= link_to "Edit", edit_card_path(@card) %>
  <% end %>
<% end %>