<h1>
  <%= link_to @cardset.name, @cardset %>
</h1>
<% @title = "#{@cardset.name}: Visual Spoiler" %>

<%= render "setviews" %>

<p>Showing all <%= pluralize @cardset.cards.count, "card" %>
</p>
<p>
  <% @cardset.cards.sort.each do |@card| %>
    <div class="CardRenderInline visualspoiler">
      <%= render :partial => "shared/prettycard", :locals => {:link => true} %>
      <%= render :partial => "shared/cardcommentcount" %>
    </div>
  <% end %>
</p>
<p><br>
  <%= render "setviews" %>
</p>