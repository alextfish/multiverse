<h1>Editing card</h1>
<% @title = "Editing #{@card.name}" %>

<%= render "form" %>

<% if permission_to?(:delete, @card.cardset) %>
  <%= link_to "Delete card", @card, :confirm => "Really DELETE #{@card.name}?", :method => :delete %> |
<% end %>
<%= link_to "Back to card", @card %> |
<%= link_to "Back to cardset", @card.cardset %>
